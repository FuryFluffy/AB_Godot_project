# Abyssal Bloom — Layer 1 Closure and Route Handoff

## Runtime-confirmed foundation

The Blood Nun victory aftermath is mandatory and uses the active Processing
Chapel background. Layer 1 is marked complete only when the aftermath reaches
one of its two route choices. Party, inventory, Bloom, progression, Resolve,
Corruption, narrative state, and the completed Layer 1 graph survive the
transaction.

The user runtime-confirmed this foundation on 2026-08-10.

## Canon topology correction

The original checkpoint temporarily treated both choices as Layer 2 entrances.
Novel canon and the user's clarification supersede that placeholder:

- **Go Up** enters the harder Layer 3 threshold.
- **Go Down** reaches Layer 2 at its exit end and starts the Jailer immediately.

Ordinary Layer 2 exploration begins later from the Bloom Refuge, not from the
Blood Nun chamber.

## Current continuation

The corrected Jailer and Refuge behavior is documented in
`JAILER_FIRST_DEFEAT_AND_BLOOM_REFUGE_ORIGIN_2026-08-10.md`.
